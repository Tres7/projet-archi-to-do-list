# Client Changesets

Use this directory only for client release notes.

Create a client changeset with:

```bash
npm --prefix client run changeset
```

The client Changesets context manages only the `client` package.
